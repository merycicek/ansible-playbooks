---
- name: Manage files
  hosts: all
  become: true
  become_method: sudo
  user: centos  
  tasks:
  - name: Manage etc motd
    lineinfile:
      path: "/etc/motd"
      regexp: "^This machine is managed by SysEng"
      line: "This machine is managed by Devops"